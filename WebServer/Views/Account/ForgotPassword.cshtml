@model ForgotPasswordViewModel

@{
    Layout = "_Layout2";
}

<div class="row align-items-center justify-content-center g-0 min-vh-100">
    <div class="col-12 col-md-8 col-lg-6 col-xxl-4 py-8 py-xl-0">
        <!-- Card -->
        <div class="card smooth-shadow-md">
            <!-- Card body -->
            <div class="card-body p-6">
                <div class="mb-4">
                    <h1> @_localizer["ForgotPassword"]</h1>
                    <p class="mb-6">
                        @_localizer["ForgotPasswordDesc"]
                    </p>
                </div>
                <!-- Form -->
                <form role="form" asp-action="ForgotPassword">
                    <input type="hidden" asp-for="ErrorMessage" />
                    <span asp-validation-for="ErrorMessage" class="text-danger"></span>
                    <!-- Email -->
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" type="email" class="form-control" placeholder="@_localizer["EnterEmail"]">
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <!-- Button -->
                    <div class="mb-3 d-grid">
                        <button type="submit" class="btn btn-primary">
                            @_localizer["Continue"]
                        </button>
                    </div>
                    <span><a href="/Account/Signin">@_localizer["ReturnLogin"]</a></span>
                </form>
            </div>
        </div>
    </div>
</div>